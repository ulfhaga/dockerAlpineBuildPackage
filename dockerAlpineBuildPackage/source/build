build() {
	# Replace with proper build command(s)
	cd "${builddir}";
                                                                                                                        mkdir -p "${pkgdir}";
}